{
  "name": "diesocketdie",
  "version": "2.0.2",
  "description": "Stress test a websocket.",
  "scripts": {
    "build": "rm -rf ./dist && babel src --out-dir dist",
    "prepublish": "rm -rf ./dist && babel src --out-dir dist",
    "pretest": "rm -rf ./dist && babel src --out-dir dist",
    "test": "./node_modules/.bin/babel-tape-runner test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/killtheliterate/diesocketdie.git"
  },
  "bin": {
    "diesocketdie": "./bin/boot"
  },
  "author": "Garrett Dawson <killtheliterate@gmail.com> (http://thedrearlight.com/)",
  "license": "ISC",
  "dependencies": {
    "debug": "^2.2.0",
    "lodash": "^4.6.1",
    "observable-socket": "^2.1.0",
    "ramda": "^0.19.1",
    "rx": "^4.1.0",
    "ws": "^1.0.1",
    "yargs": "^13.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-tape-runner": "^2.0.1",
    "tape": "^4.5.1"
  }
}
